# Legacy pre-V2-1 Vite SPA — superseded by the Next.js App Router (src/app/).
Kept for reference only; not part of the active build. See App.tsx's own import
paths (./components/Navbar etc, flat, no /landing subfolder) — this predates the
route-group migration described in the V2-1 sprint.
